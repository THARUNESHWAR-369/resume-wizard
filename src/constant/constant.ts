

export enum cookiesExpireConstant{
    ONE_DAY = 86400000, // hours * minutes * seconds * milliseconds
    ONE_WEEK = 604800000, // hours * minutes * seconds * milliseconds
}

export enum buttonCss {
  LOGOUT_CSS = "rounded-md bg-green-600 px-10 py-3 text-white shadow-sm hover:bg-green-700",
  
}
